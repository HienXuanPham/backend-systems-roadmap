# Progress Log

## 2026-08-12
- Reviewed: Go syntax and my TCP Server project.
- I learned that a listening socket is the server-side endpoint. The server program creates a socket and binds it to `IP:port`, such as `127.0.0.1:8080`. It sits at that endpoint and waits for incoming connection requests from client-side endpoints.